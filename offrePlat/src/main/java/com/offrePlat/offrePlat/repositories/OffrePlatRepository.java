package com.offrePlat.offrePlat.repositories;

import com.offrePlat.offrePlat.entities.OffrePlat;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OffrePlatRepository extends JpaRepository<OffrePlat,Long> {
}
